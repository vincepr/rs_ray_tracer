/// (x,y,z,0) - vector
/// (x,y,z,1) - point

pub enum Tupl {
    Point(u32, u32, u32),
        Vector(crate::structs::Point),
}
impl Tupl::Point {
    
}





#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn one_chars_longs() {

    }
}